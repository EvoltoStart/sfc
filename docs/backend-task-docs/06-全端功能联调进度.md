# 全端功能联调进度

更新时间：2026-04-29

## 1. 本次检查结论

当前项目已经完成“本地核心主链路”的自动化联调打通，但不能表述为全部生产化完成。本阶段按“双阶段推进 + 核心闭环优先”执行，只证明登录、司机认证、车辆、发布、搜索、申请、接单、支付、履约、安全、钱包、后台核查这一条本地闭环可重复运行。

Done：

1. 新增 `scripts/full_chain_smoke.py`，覆盖后端健康检查、乘客/车主 fake 微信登录、驾驶证、车辆、价格预览、顺路度预览、发布行程、搜索匹配、行程详情、同行申请、接单、订单、支付单、本地 mock 微信支付回调、安全联系人、安全配置、分享链接、SOS、轨迹点、轨迹摘要、上车/到达确认、钱包余额、钱包流水、提现入口、后台工作台、订单、风控、财务、用户、审核和操作审计接口。
2. 新增 `scripts/run-full-chain-local.ps1`，统一按本地端口约定启动或复用后端、客户端 Web、后台 Web，并触发全链路脚本；默认端口为后端 `127.0.0.1:8080`、客户端 Web/支付代理 `127.0.0.1:4174`、后台 Web `127.0.0.1:5174`。
3. API 全链路已通过：`python scripts\full_chain_smoke.py --base-url http://127.0.0.1:8080`，报告为 `artifacts/full-chain-report-20260429-095144.json`，摘要为 `artifacts/full-chain-summary-20260429-095144.md`。
4. 本地一键联调已通过：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-full-chain-local.ps1 -StopOnExit`，报告为 `artifacts/full-chain-report-20260429-095813.json`，摘要为 `artifacts/full-chain-summary-20260429-095813.md`。
5. 客户端 Web 浏览器烟测已通过：可使用 fake code 登录，首页、订单、支付、安全中心、钱包等关键入口可见；安全中心可见安全配置、SOS、分享和轨迹相关入口。
6. 后台管理 Web 浏览器烟测已通过：后台会话可恢复，工作台、订单中心、安全与风控、财务中心、用户管理、操作审计关键页面可加载真实数据。
7. 修复后台操作审计/订单时间线重复 React key 的页面稳定性问题，并兼容后端状态日志的 PascalCase 字段，避免订单详情时间线显示为“状态更新 · -”。
8. 小程序 README 已对齐本地联调端口，补充微信开发者工具手动验收清单。

Todo：

1. 客户端优惠券仍是静态展示，后端没有客户端优惠券契约和营销券接口。
2. 后台报表页“导出任务队列”仍为占位，后端缺少异步导出任务接口、任务状态、文件生成和清理链路。
3. 小程序尚未在微信开发者工具中完成真实点击联调；当前只完成静态结构校验和 README 手动验收清单补充。
4. 当前支付链路为本地 mock 微信支付回调或支付宝沙箱/Mock 网关能力，尚未接入真实微信支付商户号、平台证书、真实退款通道和生产回调验签。
5. MySQL Store、短信/订阅消息、监控、部署、CI/CD、生产配置检查仍未完成。

Next：

1. 用微信开发者工具导入 `frontend/client-miniapp`，关闭合法域名校验，按 README 的乘客和车主清单跑真实小程序点击联调。
2. 设计客户端优惠券接口契约，再决定是否接入营销模块或继续从导航移除。
3. 设计后台异步导出任务模型和接口，替换当前报表页占位。
4. 进入生产化阶段：MySQL Store、真实微信支付/退款闭环、短信/订阅消息、监控告警、CI/CD 和部署清单。

## 2. 本次验证记录

Done：

1. `server`：`go test ./...` 通过。
2. `frontend/client-app`：`npm run lint` 通过，`npm run build` 通过。
3. `frontend/admin-web`：`npm run lint` 通过，`npm run build` 通过。
4. `frontend/client-miniapp`：`node frontend\client-miniapp\scripts\verify-miniapp.js` 通过，校验 12 个页面、18 个 JSON 文件、15 个 WXML 文件。
5. `scripts/full_chain_smoke.py`：`python -m py_compile scripts\full_chain_smoke.py` 通过。
6. API 全链路：`python scripts\full_chain_smoke.py --base-url http://127.0.0.1:8080` 通过，`FULL_CHAIN_PASSED=true`。
7. 本地一键联调：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-full-chain-local.ps1 -StopOnExit` 通过，`FULL_CHAIN_PASSED=true`。
8. 浏览器烟测客户端 Web：`http://127.0.0.1:4174` 登录 `passenger-alpha-browser` 后，首页、订单、支付、安全中心、钱包入口可见，安全中心关键文案可见。
9. 浏览器烟测后台 Web：`http://127.0.0.1:5174` 后台会话可用，工作台、订单、风控、财务、用户、审计页面关键内容可见；修复后订单详情状态时间线显示 `PENDING_PASSENGER_PAY`、`PENDING_DEPART`、`IN_PROGRESS`、`COMPLETED`，不再显示旧兜底“状态更新 · -”。

Todo：

1. 尚未执行微信开发者工具里的小程序运行时网络请求，因为需要手动导入小程序项目并使用微信开发者工具环境。
2. 尚未执行真实第三方支付、真实地图服务、短信/订阅消息、MySQL Store 和生产部署验证。

Next：

1. 将 `artifacts/full-chain-report-*.json` 中的失败诊断结构作为后续回归入口，未来新增接口时同步加入全链路脚本。
2. 将本地 mock 联调和生产化联调分开记录，避免把 fake 登录、mock 支付和 SQLite 快照描述成生产能力。
